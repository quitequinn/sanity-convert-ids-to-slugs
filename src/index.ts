/**
 * Sanity Convert IDs to Slugs - Main Export
 * Convert document IDs to human-readable slugs with customizable generation
 */

export { default as ConvertIdsToSlugs } from './ConvertIdsToSlugs'
export { default } from './ConvertIdsToSlugs'

// Export types if needed
export type {
  ConvertIdsToSlugsProps,
  ConversionResult
} from './ConvertIdsToSlugs'